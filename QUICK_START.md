# 🚀 Быстрый старт

## Как запустить проект

### Вариант 1: Python (рекомендуется)

1. Откройте терминал в папке проекта
2. Запустите сервер:
   ```bash
   # Python 3
   python -m http.server 8000
   
   # Или Python 2
   python -m SimpleHTTPServer 8000
   ```
3. Откройте в браузере: `http://localhost:8000`

### Вариант 2: Node.js (npx serve)

1. Установите Node.js (если еще не установлен)
2. В терминале выполните:
   ```bash
   npx serve .
   ```
3. Откройте адрес, который покажет serve (обычно `http://localhost:3000`)

### Вариант 3: VS Code Live Server

1. Установите расширение "Live Server" в VS Code
2. Правой кнопкой на `index.html` → "Open with Live Server"

### Вариант 4: PHP (если установлен)

```bash
php -S localhost:8000
```

## ⚠️ Важно!

**НЕ открывайте `index.html` напрямую через `file://`** - это не сработает из-за ES6 modules и CORS политики.

Обязательно используйте локальный сервер!

## 🔍 Проверка работы

После запуска сервера:

1. Откройте `http://localhost:8000` в браузере
2. Должна загрузиться страница с интерфейсом Spool Manager
3. Проверьте консоль браузера (F12) на наличие ошибок
4. Приложение должно подключиться к Firebase

## 🐛 Решение проблем

### Ошибка "Failed to load module"
- Убедитесь, что используете локальный сервер (не file://)
- Проверьте, что все файлы в папке `src/` на месте

### Ошибка подключения к Firebase
- Проверьте интернет-соединение
- Убедитесь, что Firebase конфигурация корректна в `src/config/constants.js`

### Белый экран
- Откройте консоль браузера (F12)
- Проверьте ошибки в консоли
- Убедитесь, что все модули загружаются

## 📱 Тестирование функций

1. **Создание записи**: Нажмите кнопку "+" или клавишу N
2. **Поиск**: Используйте поле поиска или Ctrl+F
3. **Массовое добавление**: Кнопка "Массовое добавление" в меню
4. **Буфер отгрузки**: Кнопка с иконкой грузовика
5. **QR сканер**: Кнопка QR рядом с поиском
6. **Экспорт/Импорт**: Меню → Экспорт/Импорт

## 🎯 Следующие шаги

После успешного запуска:
- Протестируйте все функции
- Проверьте работу в разных браузерах
- Настройте Firebase Security Rules (если нужно)

